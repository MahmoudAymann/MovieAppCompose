package com.mdi.movie.core.utils

/*
* For Ui model classes just to identify them
* */
@Target(AnnotationTarget.CLASS) // Apply to classes only
@Retention(AnnotationRetention.SOURCE) // Retain only in source code, not in bytecode or runtime
annotation class UIModel